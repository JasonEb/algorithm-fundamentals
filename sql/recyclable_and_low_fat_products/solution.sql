-- Recyclable and Low Fat Products — https://leetcode.com/problems/recyclable-and-low-fat-products/
--
-- Write a solution to find the ids of products that are both low fat and
-- recyclable. Return the result table in any order.
--
-- Run it locally with:
--   sqlite3
--   .read schema.sql
--   .read solution.sql

SELECT product_id
FROM Products
WHERE 1 = 0; -- TODO: replace with the real condition
